# Enter the Slack OAuth-Access-Token (see README for more information).
SLACK_TOKEN=

# Choose a port where the built-in server should listen on.
SIPGATE_WEBHOOK_SERVER_PORT=

# Use the service localhost.run or ngrok which set up a reverse ssh tunnel that forwards traffic from a public URL to your localhost.
# Enter the public URL that was generated by localhost.run or ngrok.
# You also need to enter this URL as a webhook URL on https://console.sipgate.com/webhooks
SIPGATE_WEBHOOK_SERVER_ADDRESS=
